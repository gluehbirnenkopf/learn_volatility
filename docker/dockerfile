FROM influxdb:2.0

EXPOSE 8086

ENV DOCKER_INFLUXDB_INIT_MODE=setup
ENV DOCKER_INFLUXDB_INIT_USERNAME=local
ENV DOCKER_INFLUXDB_INIT_PASSWORD=local123!
ENV DOCKER_INFLUXDB_INIT_ORG=moon
ENV DOCKER_INFLUXDB_INIT_BUCKET=marketdata
ENV DOCKER_INFLUXDB_INIT_ADMIN_TOKEN="9283hdsiuh9843JHGGH9382lLPO!"